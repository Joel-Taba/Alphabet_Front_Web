import { i as useLanguage, r as format } from "./LanguageContext-IeRxb9no.js";
import { t as AmaniMascot } from "./AmaniMascot-A-Eqgs-C.js";
import { n as awardRestartBonus, t as awardCompletion } from "./progress-DCH5vN8F.js";
import { t as LetterTraceCell } from "./LetterTraceCell-D2weATol.js";
import { t as ExerciseCompletePopup } from "./ExerciseCompletePopup-Df5hR0N2.js";
import { t as getLetterFormation } from "./letter-style-resolver-CcmIqGzX.js";
import { a as useSignSpeech } from "./useSignSpeech-MSIU-G__.js";
import { t as useWritingStyle } from "./useWritingStyle-CKn53fOs.js";
import { o as nextWordGroupAfterCrossword } from "./word-catalog-C-fKc0oN.js";
import { useCallback, useEffect, useMemo, useState } from "react";
import { useNavigate } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { Check, Sparkles, Volume2 } from "lucide-react";
//#region src/components/amani/CrosswordPlay.tsx
/** Clés de toutes les cases occupées par un mot placé. */
function wordCellKeys(entry) {
	return entry.word.fr.split("").map((_, i) => {
		return `${entry.direction === "across" ? entry.row : entry.row + i},${entry.direction === "across" ? entry.col + i : entry.col}`;
	});
}
/** Palette cyclique pour les pastilles d'indices, façon jeu coloré. */
var CLUE_PALETTE = [
	{
		bg: "#EAF1FB",
		text: "#2D6BBF",
		badge: "#4A90E2"
	},
	{
		bg: "#EFF6E9",
		text: "#5E8E3E",
		badge: "#8FBF6F"
	},
	{
		bg: "#FBF1DE",
		text: "#8A6800",
		badge: "#D9A84A"
	}
];
/**
* `puzzleId`/`level` ne sont fournis que par l'étape du parcours (voir
* exercice.mots-croises.$puzzleId.tsx) : c'est ce qui déclenche l'attribution
* de points et le pop-up de fin d'exercice. Le mode libre de la bibliothèque
* (grilles régénérées à la demande, non liées au parcours) omet ces props et
* ne déclenche donc ni l'un ni l'autre, volontairement.
*/
function CrosswordPlay({ crossword, puzzleId, level }) {
	const { speak } = useSignSpeech();
	const { t, lang } = useLanguage();
	const navigate = useNavigate();
	const writingStyle = useWritingStyle();
	const [solved, setSolved] = useState(/* @__PURE__ */ new Set());
	const [justFinished, setJustFinished] = useState(false);
	const [showCompletePopup, setShowCompletePopup] = useState(false);
	const [restartKey, setRestartKey] = useState(0);
	const [awaitingRepeatCompletion, setAwaitingRepeatCompletion] = useState(false);
	const { cells, sequence, clues, mysteryWord } = useMemo(() => {
		const byKey = /* @__PURE__ */ new Map();
		const seq = [];
		const clueList = [];
		let mystery;
		for (const entry of crossword.placed) {
			if (entry.mystery) mystery = entry;
			clueList.push({
				number: entry.number,
				wordId: entry.word.id
			});
			entry.word.fr.split("").forEach((char, i) => {
				const row = entry.direction === "across" ? entry.row : entry.row + i;
				const col = entry.direction === "across" ? entry.col + i : entry.col;
				const key = `${row},${col}`;
				if (byKey.has(key)) return;
				const cell = {
					row,
					col,
					char,
					number: i === 0 ? entry.number : void 0,
					mystery: entry.mystery
				};
				byKey.set(key, cell);
				seq.push(cell);
			});
		}
		clueList.sort((a, b) => a.number - b.number);
		return {
			cells: Array.from(byKey.values()),
			sequence: seq,
			clues: clueList,
			mysteryWord: mystery
		};
	}, [crossword]);
	const activeCell = sequence.find((c) => !solved.has(`${c.row},${c.col}`));
	const allSolved = solved.size === sequence.length && sequence.length > 0;
	useEffect(() => {
		if (!allSolved || !puzzleId) return;
		awardCompletion({
			typeEtape: "MOTS_CROISES",
			modalite: "EXERCICE",
			etapeCode: puzzleId,
			palier: lang === "fr" ? 4 : 3
		});
	}, [
		allSolved,
		puzzleId,
		lang
	]);
	useEffect(() => {
		if (allSolved && awaitingRepeatCompletion) {
			awardRestartBonus();
			setAwaitingRepeatCompletion(false);
		}
	}, [allSolved, awaitingRepeatCompletion]);
	useEffect(() => {
		if (!allSolved || level == null || mysteryWord) return;
		const timer = setTimeout(() => setShowCompletePopup(true), 300);
		return () => clearTimeout(timer);
	}, [
		allSolved,
		level,
		mysteryWord
	]);
	const nextWordGroup = level != null ? nextWordGroupAfterCrossword(level) : void 0;
	const { solvedWordsCount, solvedWordCellKeys } = useMemo(() => {
		const keys = /* @__PURE__ */ new Set();
		let count = 0;
		for (const entry of crossword.placed) {
			const wordKeys = wordCellKeys(entry);
			if (wordKeys.every((k) => solved.has(k))) {
				count++;
				wordKeys.forEach((k) => keys.add(k));
			}
		}
		return {
			solvedWordsCount: count,
			solvedWordCellKeys: keys
		};
	}, [crossword, solved]);
	const totalWords = crossword.placed.length;
	const handleCellSolved = useCallback((key) => {
		setSolved((prev) => {
			const next = new Set(prev).add(key);
			if (next.size === sequence.length) setTimeout(() => setJustFinished(true), 300);
			return next;
		});
	}, [sequence.length]);
	const cellByPos = new Map(cells.map((c) => [`${c.row},${c.col}`, c]));
	const cellSize = crossword.cols >= 8 ? 40 : crossword.cols >= 6 ? 48 : 60;
	return /* @__PURE__ */ jsxs("div", {
		className: "flex flex-col items-center gap-5 w-full",
		children: [
			/* @__PURE__ */ jsxs("div", {
				className: "w-full max-w-sm rounded-[24px] p-4 shadow-sm flex items-center gap-3.5 text-white",
				style: { background: "linear-gradient(135deg, #4A90E2 0%, #2D6BBF 100%)" },
				children: [/* @__PURE__ */ jsx(AmaniMascot, {
					pose: allSolved ? "celebration" : "curiosite",
					size: "small"
				}), /* @__PURE__ */ jsxs("div", {
					className: "flex-1 min-w-0",
					children: [
						/* @__PURE__ */ jsx("p", {
							className: "text-[14px] font-bold leading-snug",
							children: allSolved ? t.motsCroises.doneTitle : t.motsCroises.hintTitle
						}),
						/* @__PURE__ */ jsx("p", {
							className: "text-[12px] text-white/80 mt-0.5",
							children: allSolved ? t.motsCroises.doneBody : t.motsCroises.hintBody
						}),
						/* @__PURE__ */ jsxs("div", {
							className: "mt-2.5 flex items-center gap-2",
							role: "progressbar",
							"aria-valuemin": 0,
							"aria-valuemax": totalWords,
							"aria-valuenow": solvedWordsCount,
							"aria-label": format(t.motsCroises.wordsFoundLabel, {
								solved: solvedWordsCount,
								total: totalWords
							}),
							children: [/* @__PURE__ */ jsx("div", {
								className: "flex-1 h-2 rounded-full bg-white/25 overflow-hidden",
								children: /* @__PURE__ */ jsx("div", {
									className: "h-full rounded-full bg-white transition-all duration-500",
									style: { width: `${totalWords ? solvedWordsCount / totalWords * 100 : 0}%` }
								})
							}), /* @__PURE__ */ jsxs("span", {
								className: "text-[11px] font-extrabold tabular-nums shrink-0",
								children: [
									solvedWordsCount,
									"/",
									totalWords
								]
							})]
						})
					]
				})]
			}),
			/* @__PURE__ */ jsx("div", {
				className: "w-full max-w-full rounded-[28px] p-3 shadow-sm overflow-x-auto",
				style: {
					background: "linear-gradient(160deg, #FBF6EC 0%, #F0E4CC 100%)",
					border: "1px solid #4A3B2A15"
				},
				children: /* @__PURE__ */ jsx("div", {
					className: "grid gap-1.5 mx-auto w-fit",
					style: { gridTemplateColumns: `repeat(${crossword.cols}, ${cellSize}px)` },
					children: Array.from({ length: crossword.rows }).map((_, row) => Array.from({ length: crossword.cols }).map((_, col) => {
						const cell = cellByPos.get(`${row},${col}`);
						if (!cell) return /* @__PURE__ */ jsx("div", { style: {
							width: cellSize,
							height: cellSize
						} }, `${row}-${col}`);
						const key = `${row},${col}`;
						const letter = getLetterFormation(cell.char, writingStyle);
						if (!letter) return /* @__PURE__ */ jsx("div", { style: {
							width: cellSize,
							height: cellSize
						} }, key);
						return /* @__PURE__ */ jsxs("div", {
							className: "relative",
							children: [
								solvedWordCellKeys.has(key) && /* @__PURE__ */ jsx("div", {
									className: "absolute -inset-1 rounded-[14px] pointer-events-none",
									style: { background: cell.mystery ? "#D9A84A2E" : "#8FBF6F2E" }
								}),
								cell.number && /* @__PURE__ */ jsx("span", {
									className: "absolute -top-1 -left-1 z-30 text-[9px] font-extrabold text-[#4A90E2] bg-white rounded-full w-4 h-4 grid place-items-center shadow-sm",
									children: cell.number
								}),
								cell.mystery && solved.has(key) && /* @__PURE__ */ jsx("span", {
									className: "absolute -top-1.5 -right-1.5 z-30 text-[#D9A84A]",
									children: /* @__PURE__ */ jsx(Sparkles, {
										className: "w-3.5 h-3.5",
										fill: "currentColor"
									})
								}),
								/* @__PURE__ */ jsx(LetterTraceCell, {
									letter,
									size: cellSize,
									isActive: activeCell?.row === row && activeCell?.col === col,
									onSolved: () => handleCellSolved(key)
								}, `${key}-r${restartKey}`)
							]
						}, key);
					}))
				})
			}),
			/* @__PURE__ */ jsx("div", {
				className: "w-full max-w-sm flex flex-col gap-2.5",
				children: clues.map(({ number, wordId }, i) => {
					const entry = crossword.placed.find((p) => p.word.id === wordId && p.number === number);
					if (!entry) return null;
					const isWordSolved = wordCellKeys(entry).every((k) => solved.has(k));
					const palette = CLUE_PALETTE[i % CLUE_PALETTE.length];
					return /* @__PURE__ */ jsxs("button", {
						type: "button",
						onClick: () => speak(entry.word.fr),
						className: "flex items-center gap-3 px-4 py-3 rounded-2xl shadow-xs active:scale-98 transition-all",
						style: { background: isWordSolved ? "#8FBF6F1F" : palette.bg },
						children: [
							/* @__PURE__ */ jsx("span", {
								className: "w-7 h-7 rounded-full text-white text-[12px] font-extrabold grid place-items-center shrink-0 transition-colors",
								style: { background: isWordSolved ? "#8FBF6F" : palette.badge },
								children: isWordSolved ? /* @__PURE__ */ jsx(Check, {
									className: "w-4 h-4",
									strokeWidth: 3
								}) : number
							}),
							/* @__PURE__ */ jsx(Volume2, {
								className: "w-4 h-4 shrink-0",
								style: { color: isWordSolved ? "#5E8E3E" : palette.badge }
							}),
							/* @__PURE__ */ jsx("span", {
								className: "text-[13px] font-semibold",
								style: { color: isWordSolved ? "#4A7A30" : palette.text },
								children: entry.direction === "across" ? t.motsCroises.across : t.motsCroises.down
							})
						]
					}, `${wordId}-${number}`);
				})
			}),
			justFinished && mysteryWord && /* @__PURE__ */ jsx("div", {
				className: "fixed inset-0 z-50 flex items-center justify-center px-6 bg-black/40 backdrop-blur-xs animate-in fade-in duration-200",
				role: "dialog",
				"aria-live": "polite",
				children: /* @__PURE__ */ jsxs("div", {
					className: "flex flex-col items-center gap-4 w-full max-w-xs rounded-3xl bg-white p-6 text-center shadow-2xl border border-gray-100 animate-in zoom-in-95 duration-300",
					children: [
						/* @__PURE__ */ jsx(AmaniMascot, {
							pose: "victoire_palier",
							size: "medium",
							priority: true
						}),
						/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("h2", {
							className: "text-[22px] font-extrabold text-[#4A3B2A]",
							children: t.motsCroises.featuredTitle
						}), /* @__PURE__ */ jsx("p", {
							className: "text-[13px] text-[#7A6A55] mt-1",
							children: t.motsCroises.featuredBody
						})] }),
						/* @__PURE__ */ jsx("div", {
							className: "flex gap-1.5",
							children: mysteryWord.word.fr.split("").map((ch, i) => /* @__PURE__ */ jsx("span", {
								className: "w-10 h-10 rounded-xl bg-[#D9A84A]/15 border-2 border-[#D9A84A] grid place-items-center text-[20px] font-extrabold text-[#8A6800]",
								children: ch
							}, i))
						}),
						/* @__PURE__ */ jsxs("button", {
							type: "button",
							onClick: () => speak(mysteryWord.word.fr),
							className: "flex items-center gap-2 px-5 py-2.5 rounded-full bg-[#D9A84A]/15 text-[#8A6800] font-bold text-sm active:scale-95 transition-transform",
							children: [
								/* @__PURE__ */ jsx(Volume2, { className: "w-4 h-4" }),
								" ",
								mysteryWord.word.fr
							]
						}),
						/* @__PURE__ */ jsx("button", {
							type: "button",
							onClick: () => {
								setJustFinished(false);
								if (level != null) setShowCompletePopup(true);
							},
							className: "w-full py-3 px-4 rounded-xl font-extrabold text-sm bg-[#8FBF6F] hover:bg-[#7AAE5A] text-white shadow-md active:scale-95 transition-all",
							children: t.motsCroises.continueLabel
						})
					]
				})
			}),
			showCompletePopup && /* @__PURE__ */ jsx(ExerciseCompletePopup, {
				onBackHome: () => navigate({ to: "/accueil" }),
				onNext: nextWordGroup ? () => navigate({
					to: "/cours/mots/$groupId",
					params: { groupId: nextWordGroup.id }
				}) : void 0,
				onRestart: () => {
					setSolved(/* @__PURE__ */ new Set());
					setJustFinished(false);
					setShowCompletePopup(false);
					setRestartKey((k) => k + 1);
					setAwaitingRepeatCompletion(true);
				}
			})
		]
	});
}
//#endregion
//#region src/lib/crosswordGenerator.ts
/** Petit générateur pseudo-aléatoire déterministe (pour des grilles reproductibles par niveau). */
function seededRandom(seed) {
	let s = seed % 2147483647;
	if (s <= 0) s += 2147483646;
	return () => (s = s * 16807 % 2147483647) / 2147483647;
}
function shuffle(arr, rand) {
	const a = [...arr];
	for (let i = a.length - 1; i > 0; i--) {
		const j = Math.floor(rand() * (i + 1));
		[a[i], a[j]] = [a[j], a[i]];
	}
	return a;
}
function attemptPlacement(candidates, targetCount, rand) {
	const grid = /* @__PURE__ */ new Map();
	const placements = [];
	let minRow = 0, maxRow = 0, minCol = 0, maxCol = 0;
	const canPlace = (chars, row, col, dir) => {
		for (let i = 0; i < chars.length; i++) {
			const r = dir === "down" ? row + i : row;
			const c = dir === "across" ? col + i : col;
			const key = `${r},${c}`;
			const existing = grid.get(key);
			if (existing) {
				if (existing !== chars[i]) return false;
			} else {
				const n1 = dir === "across" ? `${r - 1},${c}` : `${r},${c - 1}`;
				const n2 = dir === "across" ? `${r + 1},${c}` : `${r},${c + 1}`;
				if (grid.has(n1) || grid.has(n2)) return false;
			}
		}
		const beforeKey = dir === "across" ? `${row},${col - 1}` : `${row - 1},${col}`;
		const afterKey = dir === "across" ? `${row},${col + chars.length}` : `${row + chars.length},${col}`;
		if (grid.has(beforeKey) || grid.has(afterKey)) return false;
		return true;
	};
	const place = (word, row, col, dir) => {
		word.fr.split("").forEach((ch, i) => {
			const r = dir === "down" ? row + i : row;
			const c = dir === "across" ? col + i : col;
			grid.set(`${r},${c}`, ch);
			minRow = Math.min(minRow, r);
			maxRow = Math.max(maxRow, r);
			minCol = Math.min(minCol, c);
			maxCol = Math.max(maxCol, c);
		});
		placements.push({
			word,
			row,
			col,
			direction: dir
		});
	};
	const first = candidates[0];
	if (!first) return null;
	place(first, 0, 0, "across");
	const used = /* @__PURE__ */ new Set([first.id]);
	for (const word of candidates.slice(1)) {
		if (placements.length >= targetCount) break;
		if (used.has(word.id)) continue;
		const chars = word.fr.split("");
		const options = [];
		for (const [key, letter] of grid.entries()) {
			const [grStr, gcStr] = key.split(",");
			const gr = Number(grStr), gc = Number(gcStr);
			for (let i = 0; i < chars.length; i++) {
				if (chars[i] !== letter) continue;
				const acrossRow = gr, acrossCol = gc - i;
				if (canPlace(chars, acrossRow, acrossCol, "across")) options.push({
					row: acrossRow,
					col: acrossCol,
					dir: "across"
				});
				const downRow = gr - i, downCol = gc;
				if (canPlace(chars, downRow, downCol, "down")) options.push({
					row: downRow,
					col: downCol,
					dir: "down"
				});
			}
		}
		if (options.length === 0) continue;
		const choice = options[Math.floor(rand() * options.length)];
		place(word, choice.row, choice.col, choice.dir);
		used.add(word.id);
	}
	if (placements.length < 2) return null;
	return {
		placements,
		minRow,
		maxRow,
		minCol,
		maxCol
	};
}
/**
* Génère une grille de mots croisés à partir d'une banque de mots : place autant de mots
* que possible (jusqu'à `targetCount`) en les faisant s'entrecroiser sur des lettres
* communes, désigne un mot mystère (sans indice, révélé à la fin), et numérote chaque
* case de départ comme dans une vraie grille de mots croisés.
*/
function generateCrossword(pool, targetCount, seed, attempts = 60) {
	const rand = seededRandom(seed);
	const usable = pool.filter((w) => w.fr.length >= 2 && w.fr.length <= 9);
	if (usable.length < 2) return null;
	let best = null;
	for (let attempt = 0; attempt < attempts; attempt++) {
		const shuffled = shuffle(usable, rand);
		shuffled.sort((a, b) => b.fr.length - a.fr.length);
		const result = attemptPlacement(attempt % 3 === 0 ? shuffled : shuffle(shuffled, rand), targetCount, rand);
		if (result && result.placements.length > (best?.placements.length ?? 0)) best = result;
		if (best && best.placements.length >= targetCount) break;
	}
	if (!best) return null;
	const offsetRow = -best.minRow;
	const offsetCol = -best.minCol;
	const normalized = best.placements.map((p) => ({
		...p,
		row: p.row + offsetRow,
		col: p.col + offsetCol
	})).sort((a, b) => a.row - b.row || a.col - b.col);
	const startNumbers = /* @__PURE__ */ new Map();
	let nextNumber = 1;
	const numbered = normalized.map((p) => {
		const key = `${p.row},${p.col}`;
		if (!startNumbers.has(key)) startNumbers.set(key, nextNumber++);
		return {
			...p,
			number: startNumbers.get(key),
			mystery: false
		};
	});
	const mysteryIdx = Math.floor(rand() * numbered.length);
	numbered[mysteryIdx].mystery = true;
	return {
		rows: best.maxRow - best.minRow + 1,
		cols: best.maxCol - best.minCol + 1,
		placed: numbered
	};
}
//#endregion
export { CrosswordPlay as n, generateCrossword as t };
